//! Render module skeleton

/// Placeholder for rendering code (drawing lanes, vehicles, UI)
pub struct Renderer {
    // add renderer state here
}

impl Renderer {
    /// Create a new renderer
    pub fn new() -> Self {
        Renderer {}
    }
}
